# User Preferences

- Never push to main/master without explicit permission
- Always use `/commit` for commits and `/pr` for pull requests
- Commit messages focus on why, not what — the code speaks for itself
